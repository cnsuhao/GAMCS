#LINK_DIRECTORIES(${PROJECT_BINARY_DIR}/src) # 库目录搜索路径: -L
AUX_SOURCE_DIRECTORY(. PRISONERDILEMMA_SRCS)
ADD_EXECUTABLE(pd ${PRISONERDILEMMA_SRCS})
TARGET_LINK_LIBRARIES(pd avatar mysqlclient pthread)   # 指定链接库
