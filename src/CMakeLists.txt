AUX_SOURCE_DIRECTORY(. LIB_SRCS)
ADD_LIBRARY(${GIMCS_NAME} SHARED ${LIB_SRCS})
TARGET_LINK_LIBRARIES(${GIMCS_NAME} ${LIBS})   # depended libraries
SET_TARGET_PROPERTIES(${GIMCS_NAME} PROPERTIES OUTPUT_NAME ${GIMCS_NAME} CLEAN_DIRECT_OUTPUT 1)

# install library
SET(LIBDIR  ${CMAKE_INSTALL_PREFIX}/lib)
INSTALL(TARGETS ${GIMCS_NAME} 
    LIBRARY DESTINATION ${LIBDIR})
